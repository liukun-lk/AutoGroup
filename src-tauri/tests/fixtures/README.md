# 测试固定数据（fixtures）

本目录有两组文件：把整条流水线钉死的 golden fixtures，以及随机化方案评审用的真实数据。

## 端到端 golden fixtures

`e2e_grouping_test.rs` 用这两个文件把整条流水线钉死：

```
e2e_input.xlsx  ──解析──▶ Dataset ──分组──▶ GroupingResult ──导出──▶ 与 e2e_expected_output.xlsx 逐格比对
```

| 文件 | 说明 |
|---|---|
| `e2e_input.xlsx` | 真实实验数据：9 只动物（6 雄 + 3 雌），双行表头，解析出 73 个指标键 |
| `e2e_expected_output.xlsx` | 上面这份输入跑出来、并且**经人工确认接受**的导出结果：3 组 × 3 只（2 雄 + 1 雌），70 个指标全部达标 |

### 2026-08-06 重新生成

新增了 `事后比较` sheet（Tukey/Dunnett 改用精确分布后，事后 P 值才值得导出）。
重生成前逐格比对确认：`分组结果`（970 格）与 `统计结果`（355 格）**零差异**，
`汇总信息` 仅 `计算耗时 (ms)` 一格不同（本来就在跳过之列）。也就是说人工确认过的那份
分组与统计数字原封不动，只是多了一张表。新表的 210 个 P 值已用
`grouping_engine.py` 交叉复核，最大偏差 1.79e-11。

### 2026-08-07 重新生成

`汇总信息` 新增「分组方法与可追溯性」一节共 10 行（应用场景 / 分组方式 / 分组原理 / 分层变量 /
随机种子 / 随机数算法 / 抽样次数 / 输入指纹 / 引擎版本，见 `docs/randomization_design.md` §8.9），
随机化路径与择优优化路径都要在导出文件里说清楚自己属于哪一类分组原理。

重生成前逐格比对确认：`分组结果`、`统计结果`、`事后比较` 三张表**零差异**，`汇总信息` 前 10 行
不变、原有的「统计配置」与「结果摘要」两节内容逐格不变，只是整体下移 10 行；唯一的数值差异是
`计算耗时 (ms)`（本来就在跳过之列）。人工确认过的分组与统计数字仍原封不动。

## e2e fixtures 做过的匿名化

除以下两项外，上面这两个 e2e 文件与真实产物完全一致——每个测量值、表头、sheet、单元格样式都没动：

1. 动物编号里的研究编号被替换：`XHP26010NN` → `DEMO0NN`（在 `sharedStrings.xml` 里整串替换，因此 `动物编号` / `样本号` / `样品识别号` / `FULLNAME` 四列同步生效）
2. `docProps` 里的 `dc:creator`、`cp:lastModifiedBy` 等文档属性被清空

指标名称（`kg`、`ALT`、`WBC(10^9/L)` 等）是通用实验室缩写，未做处理；数值必须保留原样，否则统计检验就失去了真实性。

## 测试覆盖到什么

- **解析**：双行表头规则、指标键、性别识别、动物数量
- **指标筛选**：73 个键中排除 3 个文本列（`样本号`、`样品识别号`、`FULLNAME`）后应为 70 个，与前端 `src/utils/indicator-filter.ts` 的默认行为一致
- **分组**：候选枚举、检验级联、排序取优，最终选中的动物分配必须与人工确认的那一版逐只相同
- **事后比较**：70 个指标各带 C(3,2) = 3 个两两比较且全部达标，另有一条"不得整片饱和成 1.0"的断言——那是精确 studentized range 被换回近似实现时的特征
- **导出**：四个 sheet（`分组结果` / `统计结果` / `事后比较` / `汇总信息`）的全部单元格。文本严格相等；数值按相对误差 `1e-9` 比对，容忍不同平台 libm 的末位差异。`计算耗时 (ms)` 这一行会跳过。

## 这个测试失败了怎么办

**默认假设是代码回归了，不是固定数据过期了。** 失败信息会直接指出 sheet、行、列和两边的值。

只有在你**有意**改变了输出（例如新增一个 sheet、调整导出列）时才更新固定数据，并且要在提交说明里写清改了什么、为什么。重新生成的方式：跑一遍应用导出新结果，再用同样的规则做匿名化：

```python
# 把 sharedStrings 里的研究编号换掉，并清空 docProps
ID_MAP = {f"XHP26010{i:02d}": f"DEMO0{i:02d}" for i in range(1, 10)}
```

如果只是想确认「这份分组在统计上到底对不对」，别改固定数据，先用精确参考实现复核：

```bash
python3 .claude/skills/animal-grouping/scripts/grouping_engine.py verify \
  --excel src-tauri/tests/fixtures/e2e_input.xlsx \
  --assignments <结果 JSON> --alpha 0.05 --mode strict --compare
```

---

## 随机化方案的真实数据（2026-08-06 加入）

| 文件 | 说明 |
|---|---|
| `randomization_input_60f.xlsx` | 实验室提供的 60 只动物（全部雌性）、2 个指标，转成双行表头格式后的版本，实测解析通过：60 只、全雌、指标键 `["体重", "CD45 比例"]` |

**两个表头识别缺陷已于 2026-08-07 修复**（`parser.rs::detect_header_row`）。原先只在前两列匹配
`动物编号/性别/AnimalID/Sex/Animal/ID` 关键词并返回*第一个*命中行，于是：实验室原始文件（第 1 列表头
`Serial#`、第 2 列性别表头为空）两列都不匹配，兜底把第一条数据行当成表头，报“没有识别到任何指标列”；
而双行表头首行若写了 `AnimalID` / `Sex`，命中的是首行，第二行（中文表头）被当作数据行，报“性别「性别」
无法识别”。

现在的判据以数据为准：**数据行的第 2 列一定是可识别的性别字面量**，表头行就是第一条数据行的上一行；
关键词匹配退化为兜底，且取*最后一个*命中行。首行前两列留空不再是必需的隐式约定。三种形态
（`Serial#` + 空性别表头、首行含 `AnimalID`/`Sex` 的双行表头、首行留空的双行表头）都有单元测试覆盖，
见 `parser.rs` 的 `tests` 模块。

这个文件本身不含可识别信息（编号是 1–60 的序号），未做匿名化处理。基于它的分析与实测结论见
`docs/randomization_design.md`，可复跑的脚本见 `docs/randomization_evidence/`。
